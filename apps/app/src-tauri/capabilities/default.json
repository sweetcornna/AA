{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the AA app window",
  "windows": ["main"],
  "permissions": ["core:default", "deep-link:default"]
}
